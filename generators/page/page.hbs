import React, { FC } from 'react'
import { View } from '@tarojs/components'
import { useReady } from '@tarojs/taro'
import './{{camelCase FILE_NAME}}.styl'

const {{pascalCase FILE_NAME}}: FC = () => {
  useReady(() => {})

	return (
		<View className='{{camelCase FILE_NAME}}-page'>
			<View>{{FILE_DESC}}</View>
		</View>
	)
}

export default {{pascalCase FILE_NAME}}
